Default molds
=============

These molds should probably be moved to a separate package that only
provides them, and only get pulled in for development/testing purposes.
This change should only happen when this package is matured and before
its first release.
